// ======================================================
//  logic.Data → normalizeData.js
//  PURPOSE: Normalize, clean, validate loaded data
// ======================================================

export default function normalizeData(data) {
    if (!data) return null;

    // Placeholder normalization (expand later)
    return data;
}